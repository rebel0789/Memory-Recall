# Protocol Agent Rules

Protocol changes are additive within v1, require compatibility fixtures, and cannot expose provider IDs as canonical identity. Unknown major versions fail clearly. Update RFC 0001 and examples with schema changes.
