# Domain Packages

Packages expose provider-neutral contracts and reference implementations. They never import external adapters. The bootstrap intentionally uses direct workspace-relative imports so it remains dependency-free and transparent.
