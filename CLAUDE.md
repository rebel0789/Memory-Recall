# Claude-compatible repository entry

Read and follow `AGENTS.md` and `ASSIGN_TO_AGENT.md`. Run `npm run status`; only run `npm run task -- <OAF-ID>` when status names a next task. This file intentionally does not duplicate the canonical instructions.
