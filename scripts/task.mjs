import { readFile } from 'node:fs/promises';
const backlog=JSON.parse(await readFile('planning/backlog.json','utf8'));
const args=process.argv.slice(2),jsonMode=args.includes('--json'),id=args.find(value=>/^OAF-\d{3}$/.test(value));
if(!id){const completed=new Set(backlog.tasks.filter(task=>task.status==='completed').map(task=>task.id));const ready=backlog.tasks.filter(task=>task.status==='planned'&&task.dependsOn.every(dep=>completed.has(dep)));if(jsonMode)console.log(JSON.stringify({ready},null,2));else{console.log('Ready tasks:');for(const task of ready)console.log(`- ${task.id} ${task.title}`);console.log('\nUse: npm run task -- <OAF-ID>')}process.exit(0)}
const task=backlog.tasks.find(item=>item.id===id);if(!task){console.error(`Unknown task: ${id}`);process.exitCode=2}else if(jsonMode)console.log(JSON.stringify(task,null,2));else{console.log(`${task.id} — ${task.title}`);console.log(`Milestone: ${task.milestone}`);console.log(`Status: ${task.status}`);console.log(`Owner role: ${task.ownerAgent}`);console.log(`Dependencies: ${task.dependsOn.join(', ')||'none'}`);console.log(`\nObjective\n${task.objective}`);console.log('\nDeliverables');task.deliverables.forEach(value=>console.log(`- ${value}`));console.log('\nRequired reading');task.requiredReading.forEach(value=>console.log(`- ${value}`));console.log('\nAcceptance commands');task.acceptanceCommands.forEach(value=>console.log(`  ${value}`));console.log(`\nStop condition\n${task.stopCondition}`)}
