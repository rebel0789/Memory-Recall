# Web Agent Rules

Follow `DESIGN.md`, shared tokens, stable URLs, semantic controls, all interface states, no inline untrusted HTML, no direct storage access, and no status communicated only by color.
