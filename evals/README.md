# Evaluations

Deterministic cases test context selection, policy, and memory-write behavior. Add a minimal regression case before changing defaults. Model-quality suites will remain separate from deterministic merge gates.
