# Documentation Agent Rules

Documentation must separate implemented, reference, experimental, specified, and unsupported behavior. Link to the authoritative contract instead of duplicating it. Use short paragraphs, examples, concrete acceptance criteria, and exact paths. Never present vendor claims as independent evidence. Update `PROJECT_STATUS.json` only with test evidence.
